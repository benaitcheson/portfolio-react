// React dependencies
import React from 'react';
import ReactDOM from 'react-dom';

// My own modules
import App from './components/App';

ReactDOM.render(
    <App />, 

    // Use github for projects and make each repo a project on this site. Scrap the internet for the info.

    document.getElementById('root')
);